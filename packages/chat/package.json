{
  "name": "@my/chat",
  "version": "0.1.0",
  "description": "The bus: one append-only TSV, a cursor per reader, and the four verbs over it.",
  "license": "MIT",
  "author": "Gabriel (biliboss)",
  "type": "module",
  "exports": {
    ".": "./chat.ts"
  },
  "dependencies": {
    "@my/interfaces": "workspace:*",
    "@my/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20",
    "typescript": "catalog:"
  },
  "scripts": {
    "check": "tsc --noEmit --strict --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --types node,bun *.ts"
  }
}
