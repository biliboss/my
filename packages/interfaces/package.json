{
  "name": "@my/interfaces",
  "version": "0.1.0",
  "description": "The contracts of the my family: one file per system, types only, no runtime.",
  "license": "MIT",
  "author": "Gabriel (biliboss)",
  "type": "module",
  "exports": {
    "./*.ts": "./*.ts",
    "./*": "./*.ts"
  },
  "devDependencies": {
    "typescript": "catalog:"
  },
  "scripts": {
    "check": "tsc --noEmit --strict --target es2022 *.ts"
  }
}
