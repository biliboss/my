{
  "name": "@biliboss/shared",
  "version": "0.1.0",
  "description": "Primitives more than one package needs: the failure shape and the argv readers.",
  "license": "MIT",
  "author": "Gabriel (biliboss)",
  "type": "module",
  "exports": {
    "./result": "./result.ts",
    "./argv": "./argv.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20",
    "typescript": "catalog:"
  },
  "scripts": {
    "check": "tsc --noEmit --strict --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --types node,bun *.ts"
  }
}
