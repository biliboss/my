{
  "name": "@my/shared",
  "version": "0.1.0",
  "description": "Primitives more than one package needs: the three roots, the failure shape, the argv readers.",
  "license": "MIT",
  "author": "Gabriel (biliboss)",
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./result": "./result.ts",
    "./paths": "./paths.ts",
    "./argv": "./argv.ts",
    "./findings": "./findings.ts",
    "./gh": "./gh.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20",
    "typescript": "catalog:"
  },
  "scripts": {
    "check": "tsc --noEmit --strict --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --types node,bun *.ts"
  },
  "dependencies": {
    "@my/interfaces": "workspace:*"
  }
}
